import React from 'react';
import './Header.css'
import Nav from '../../Components/Nav/Nav.js';

const Header = () => {
    return (
        <header>
            <Nav />
        </header>
    );
}

export default Header;
